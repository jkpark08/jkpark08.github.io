---
layout: project-page
title: Controllable ISP
home_section: project
project_subtitle: with Samsung AI Center
img: scisp/scisp.png
project_script: /assets/js/style-explorer.js
---

# Controllable ISP

Here we provide interactive control over foreground and background styles while considering harmony between two conflicting styles.

<section
  class="style-explorer"
  id="style-explorer"
  data-image-base="{{ '/assets/img/scisp/' | relative_url }}">

  <div class="style-explorer__header">
    <p class="project-venue">INTERACTIVE STYLE EXPLORER</p>
    <h2>Foreground and Background Style Control</h2>
    <p>
      Select a background style and a foreground style to visualize
      the corresponding generated result.
    </p>
  </div>

  {% assign background_codes = "03,05,10,23" | split: "," %}
  {% assign foreground_codes = "03,15,16,17,20" | split: "," %}

  <div class="style-explorer__layout">

    <div class="style-control-panel">
      <p class="style-control-panel__title">Background Style</p>

      <div class="style-option-grid style-option-grid--background">
        {% for bg in background_codes %}
        <button
          class="style-option style-option--background {% if forloop.first %}is-active{% endif %}"
          type="button"
          data-background="{{ bg }}"
          aria-pressed="{% if forloop.first %}true{% else %}false{% endif %}">

          <img
            src="{{ '/assets/img/scisp/output_' | append: bg | append: '_03.png' | relative_url }}"
            alt="Background style {{ forloop.index }}">

          <span>Background {{ forloop.index }}</span>
        </button>
        {% endfor %}
      </div>
    </div>

    <div class="style-result-panel">
      <div class="style-result-panel__top">
        <div>
          <p class="style-result-panel__label">Selected Result</p>
          <h3 id="style-selection-text">
            Background 1 × Foreground 1
          </h3>
        </div>
      </div>

      <div class="style-result-image-wrap">
        <img
          id="style-result-image"
          src="{{ '/assets/img/scisp/output_03_03.png' | relative_url }}"
          alt="Generated result with Background 1 and Foreground 1">
      </div>
    </div>

    <div class="style-control-panel">
      <p class="style-control-panel__title">Foreground Style</p>

      <div class="style-option-grid style-option-grid--foreground">
        {% for fg in foreground_codes %}
        <button
          class="style-option style-option--foreground {% if forloop.first %}is-active{% endif %}"
          type="button"
          data-foreground="{{ fg }}"
          aria-pressed="{% if forloop.first %}true{% else %}false{% endif %}">

          <img
            src="{{ '/assets/img/scisp/output_03_' | append: fg | append: '.png' | relative_url }}"
            alt="Foreground style {{ forloop.index }}">

          <span>Foreground {{ forloop.index }}</span>
        </button>
        {% endfor %}
      </div>
    </div>

  </div>
</section>
